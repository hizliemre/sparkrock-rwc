<Project Sdk="Aspire.AppHost.Sdk/13.1.0">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <UserSecretsId>b9c41a98-f987-4292-b35b-75416f5a75a6</UserSecretsId>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Aspire.Hosting.AppHost" />
      <PackageReference Include="Aspire.Hosting.PostgreSQL" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\api\api.csproj" />
      <!-- IsAspireProjectResource=false: the Aspire SDK treats ProjectReference as a resource to
           orchestrate, not an assembly to link. This one is a plain library reference, so the
           AppHost can use the opt-in key constant rather than duplicating the string. -->
      <ProjectReference Include="..\service.defaults\service.defaults.csproj" IsAspireProjectResource="false" />
    </ItemGroup>

</Project>
