# ValidationProblemDetails serialises its errors as a JSON object at the same pointer the shared
# envelope uses for an array of {source, path, code, message}. One key with two shapes is worse than
# the defect it replaced, so the collision is made unrepeatable rather than merely fixed once.
T:Microsoft.AspNetCore.Mvc.ValidationProblemDetails;Write a plain ProblemDetails with `violations` in Extensions (conventions §2).
M:Microsoft.AspNetCore.Http.Results.ValidationProblem(System.Collections.Generic.IDictionary{System.String,System.String[]},System.String,System.String,System.Nullable{System.Int32},System.String,System.Collections.Generic.IDictionary{System.String,System.Object});Write a plain ProblemDetails with `violations` in Extensions (conventions §2).

# Clock reads belong behind TimeProvider.
P:System.DateTime.Now;Use TimeProvider.
P:System.DateTimeOffset.Now;Use TimeProvider.
